selectQnaAll=SELELCT * FROM (SELECT ROWNUM AS RNUM Q.* FROM (SELECT * FROM PRODUCT_QNA WHERE PRODUCT_NAME=? ORDER BY QNA_ENROLL_DATE DESC) Q) WHERE RNUM BETWEEN ? AND ?
selectQnaAllCount=SELECT COUNT(*) FROM PRODUCT_QNA WHERE PRODUCT_NAME=?